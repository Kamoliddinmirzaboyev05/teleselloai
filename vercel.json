{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "if [ -f frontend-admin/package-lock.json ]; then cd frontend-admin && npm ci; else npm ci; fi",
  "buildCommand": "if [ -f frontend-admin/package.json ]; then cd frontend-admin && npm run build && rm -rf ../dist && cp -R dist ../dist; else npm run build; fi",
  "env": {
    "NODE_VERSION": "22"
  },
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://13.60.104.64/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
